# Rust
/core_rust/target/
**/*.pdb
**/*.dll
**/*.lib
**/*.exp

# Flutter
/app_flutter/.dart_tool/
/app_flutter/build/
/app_flutter/.flutter-plugins*
/app_flutter/.metadata
# Note: app_flutter/windows/flutter/ is intentionally tracked (Flutter engine CMake stubs)

# PJSIP builds (compiled output only — source is committed under engine_pjsip/pjproject/)
/engine_pjsip/build/

# Misc
.DS_Store

# CodeQL tooling artifact
_codeql_detected_source_root
