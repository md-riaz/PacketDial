# Run with `dart run ffigen --config ffigen.yaml`.
name: VoipBridgeBindings
description: |
  Bindings for `src/voip_bridge.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/voip_bridge_bindings_generated.dart'
headers:
  entry-points:
    - 'src/voip_bridge.h'
  include-directives:
    - 'src/voip_bridge.h'
ffi-native:
comments:
  style: any
  length: full
