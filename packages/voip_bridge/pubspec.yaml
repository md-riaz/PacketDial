name: voip_bridge
description: PacketDial FFI bridge contract and native adapters.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.1

dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0
  logging: ^1.3.0
  meta: ^1.15.0
  native_toolchain_c: ^0.17.4
  ffi: ^2.1.4
  uuid: ^4.5.1

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
