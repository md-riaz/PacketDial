# Flutter App

This folder is intentionally minimal in scaffold form.

To generate a full Flutter desktop project structure, run:

```powershell
cd app_flutter
flutter create .
```
Then restore the `lib/` folder content from this repo (it already exists),
and ensure `voip_core.dll` is copied into `windows/runner/` (scripts handle this).
