Place the iOS native engine artifact here for mobile runtime support.

Recommended options:

- `voip_core.xcframework`
- a framework/static library integrated into the Runner target

`ReferenceEngineVoipBridge` expects the iOS engine symbols to be linked into
the process image.
