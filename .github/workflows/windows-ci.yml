name: windows-ci

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: windows-2025
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Build Windows app
        uses: ./.github/actions/build-windows
